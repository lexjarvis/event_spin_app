import React from 'react';

const Spinner = () => {
  return (
    <div className="spinner">
      {/* Your spinner UI here */}
      <div className="spinner-wheel">Spinner Wheel</div>
      <button>Spin</button>
    </div>
  );
};

export default Spinner;
