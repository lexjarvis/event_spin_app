import React from 'react';
import './EventDetails.css'; // Replace with the actual path to your CSS file

const EventDetails = ({ event }) => {
  return (
    <div className="event-details">
      {/* Render event details here */}
    </div>
  );
};

export default EventDetails;
